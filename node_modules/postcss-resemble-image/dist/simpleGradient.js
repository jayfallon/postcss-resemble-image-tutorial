'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = simpleGradient;
function simpleGradient(colourStops) {
    return 'linear-gradient(90deg, ' + colourStops.map(function (stop) {
        return stop.colour + ' ' + stop.position + '%';
    }).join(', ') + ')';
}