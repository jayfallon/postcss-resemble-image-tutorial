'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = complexGradient;
function complexGradient(colourStops) {
    return 'linear-gradient(90deg, ' + colourStops.map(function (stop, index) {
        var colour = '';
        if (index) {
            colour = colourStops[index - 1].colour + ' ' + stop.position + '%, ';
        }
        return colour + (stop.colour + ' ' + stop.position + '%');
    }).join(', ') + ')';
}